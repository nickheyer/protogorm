// Code generated by protogorm. DO NOT EDIT.

package db

import (
	"context"
	"errors"
	"fmt"
	"time"

	testv1 "example.test/gen/test/v1"
	"github.com/google/uuid"
	"gorm.io/gorm"
	"gorm.io/gorm/clause"
)

// Inserts rows into authors
func (s *Store) CreateAuthor(ctx context.Context, rows ...*testv1.Author) error {
	if len(rows) == 0 {
		return nil
	}
	for _, r := range rows {
		if r.Id == "" {
			r.Id = uuid.New().String()
		}
	}
	return s.db.WithContext(ctx).Omit(clause.Associations).Create(rows).Error
}

// Loads one row from authors
func (s *Store) GetAuthor(ctx context.Context, id string) (*testv1.Author, error) {
	var row testv1.Author
	if err := s.db.WithContext(ctx).First(&row, "id = ?", id).Error; err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return nil, fmt.Errorf("author not found: %w", gorm.ErrRecordNotFound)
		}
		return nil, err
	}
	return &row, nil
}

// Lists rows from authors
func (s *Store) ListAuthors(ctx context.Context) ([]*testv1.Author, error) {
	var rows []*testv1.Author
	if err := s.db.WithContext(ctx).Order("created_at DESC").Find(&rows).Error; err != nil {
		return nil, err
	}
	return rows, nil
}

// Saves one row into authors
func (s *Store) UpdateAuthor(ctx context.Context, row *testv1.Author) error {
	return s.db.WithContext(ctx).Omit(clause.Associations).Save(row).Error
}

// Patches columns on one authors row
func (s *Store) UpdateAuthorFields(ctx context.Context, id string, fields map[string]any) error {
	if len(fields) == 0 {
		return nil
	}
	res := s.db.WithContext(ctx).Model(&testv1.Author{}).Where("id = ?", id).Updates(fields)
	if res.Error != nil {
		return res.Error
	}
	if res.RowsAffected == 0 {
		return fmt.Errorf("author not found: %w", gorm.ErrRecordNotFound)
	}
	return nil
}

// Deletes one row from authors
func (s *Store) DeleteAuthor(ctx context.Context, id string) error {
	return s.db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
		var parent testv1.Author
		if err := tx.First(&parent, "id = ?", id).Error; err != nil {
			return err
		}
		if err := tx.Delete(&testv1.Book{}, "author_id = ?", id).Error; err != nil {
			return err
		}
		if err := tx.Delete(&testv1.Review{}, "author_email = ?", parent.Email).Error; err != nil {
			return err
		}
		return tx.Delete(&testv1.Author{}, "id = ?", id).Error
	})
}

// Counts rows in authors
func (s *Store) CountAuthors(ctx context.Context) (int64, error) {
	var count int64
	err := s.db.WithContext(ctx).Model(&testv1.Author{}).Count(&count).Error
	return count, err
}

// Inserts or refreshes one authors row
func (s *Store) UpsertAuthor(ctx context.Context, row *testv1.Author) error {
	if row.Id == "" {
		row.Id = uuid.New().String()
	}
	return s.db.WithContext(ctx).Omit(clause.Associations).Clauses(clause.OnConflict{
		Columns:   []clause.Column{{Name: "id"}},
		DoUpdates: clause.AssignmentColumns([]string{"email", "name", "api_key", "updated_at"}),
	}).Create(row).Error
}

// Loads one row from authors
func (s *Store) GetAuthorByEmail(ctx context.Context, email string) (*testv1.Author, error) {
	var row testv1.Author
	if err := s.db.WithContext(ctx).Where("email = ?", email).First(&row).Error; err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return nil, nil
		}
		return nil, err
	}
	return &row, nil
}

// Inserts rows into books
func (s *Store) CreateBook(ctx context.Context, rows ...*testv1.Book) error {
	if len(rows) == 0 {
		return nil
	}
	for _, r := range rows {
		if r.Id == "" {
			r.Id = uuid.New().String()
		}
	}
	return s.db.WithContext(ctx).Omit(clause.Associations).Create(rows).Error
}

// Loads one row from books
func (s *Store) GetBook(ctx context.Context, id string) (*testv1.Book, error) {
	var row testv1.Book
	if err := s.db.WithContext(ctx).First(&row, "id = ?", id).Error; err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return nil, fmt.Errorf("book not found: %w", gorm.ErrRecordNotFound)
		}
		return nil, err
	}
	return &row, nil
}

// Lists rows from books
func (s *Store) ListBooks(ctx context.Context) ([]*testv1.Book, error) {
	var rows []*testv1.Book
	if err := s.db.WithContext(ctx).Order("title ASC").Find(&rows).Error; err != nil {
		return nil, err
	}
	return rows, nil
}

// Saves one row into books
func (s *Store) UpdateBook(ctx context.Context, row *testv1.Book) error {
	return s.db.WithContext(ctx).Omit(clause.Associations).Save(row).Error
}

// Patches columns on one books row
func (s *Store) UpdateBookFields(ctx context.Context, id string, fields map[string]any) error {
	if len(fields) == 0 {
		return nil
	}
	res := s.db.WithContext(ctx).Model(&testv1.Book{}).Where("id = ?", id).Updates(fields)
	if res.Error != nil {
		return res.Error
	}
	if res.RowsAffected == 0 {
		return fmt.Errorf("book not found: %w", gorm.ErrRecordNotFound)
	}
	return nil
}

// Deletes one row from books
func (s *Store) DeleteBook(ctx context.Context, id string) error {
	var refsReview int64
	if err := s.db.WithContext(ctx).Model(&testv1.Review{}).Where("book_id = ?", id).Count(&refsReview).Error; err != nil {
		return err
	}
	if refsReview > 0 {
		return fmt.Errorf("book still referenced by %d review rows", refsReview)
	}
	return s.db.WithContext(ctx).Delete(&testv1.Book{}, "id = ?", id).Error
}

// Counts rows in books
func (s *Store) CountBooks(ctx context.Context) (int64, error) {
	var count int64
	err := s.db.WithContext(ctx).Model(&testv1.Book{}).Count(&count).Error
	return count, err
}

// Lists rows from books
func (s *Store) ListBooksByAuthor(ctx context.Context, authorID string, offset, limit int) ([]*testv1.Book, int64, error) {
	var total int64
	counter := s.db.WithContext(ctx).Model(&testv1.Book{})
	counter = counter.Where("author_id = ?", authorID)
	if err := counter.Count(&total).Error; err != nil {
		return nil, 0, err
	}
	var rows []*testv1.Book
	q := s.db.WithContext(ctx)
	q = q.Preload("Author")
	q = q.Where("author_id = ?", authorID)
	q = q.Order("published_at DESC")
	q = q.Offset(offset)
	if limit > 0 {
		q = q.Limit(limit)
	}
	if err := q.Find(&rows).Error; err != nil {
		return nil, 0, err
	}
	return rows, total, nil
}

// Lists rows from books
func (s *Store) ListBooksByGenre(ctx context.Context, genre testv1.Genre) ([]*testv1.Book, error) {
	var rows []*testv1.Book
	q := s.db.WithContext(ctx)
	q = q.Where("genre = ?", genre)
	q = q.Order("title ASC")
	q = q.Limit(25)
	if err := q.Find(&rows).Error; err != nil {
		return nil, err
	}
	return rows, nil
}

// Lists rows from books
func (s *Store) ListRecentBooks(ctx context.Context, since time.Time, limit int) ([]*testv1.Book, error) {
	var rows []*testv1.Book
	q := s.db.WithContext(ctx)
	q = q.Where("datetime(published_at) > datetime(?)", since)
	q = q.Order("title ASC")
	if limit > 0 {
		q = q.Limit(limit)
	}
	if err := q.Find(&rows).Error; err != nil {
		return nil, err
	}
	return rows, nil
}

// Counts matching rows in books
func (s *Store) CountBooksByAuthor(ctx context.Context, authorID string) (int64, error) {
	var count int64
	err := s.db.WithContext(ctx).Model(&testv1.Book{}).Where("author_id = ?", authorID).Count(&count).Error
	return count, err
}

// Reports whether a matching books row exists
func (s *Store) BookExistsByTitle(ctx context.Context, title string) (bool, error) {
	var count int64
	err := s.db.WithContext(ctx).Model(&testv1.Book{}).Where("title = ?", title).Count(&count).Error
	return count > 0, err
}

// Deletes rows from books
func (s *Store) PurgeBooksByAuthor(ctx context.Context, authorID string) error {
	res := s.db.WithContext(ctx).Where("author_id = ?", authorID).Delete(&testv1.Book{})
	if res.Error != nil {
		return res.Error
	}
	if res.RowsAffected == 0 {
		return fmt.Errorf("book not found: %w", gorm.ErrRecordNotFound)
	}
	return nil
}

// Patches matching rows in books
func (s *Store) RetitleBook(ctx context.Context, title string, id string) error {
	res := s.db.WithContext(ctx).Exec("UPDATE books SET title = ? WHERE id = ?", title, id)
	if res.Error != nil {
		return res.Error
	}
	if res.RowsAffected == 0 {
		return fmt.Errorf("book not found: %w", gorm.ErrRecordNotFound)
	}
	return nil
}

// Inserts rows into reviews
func (s *Store) CreateReview(ctx context.Context, rows ...*testv1.Review) error {
	if len(rows) == 0 {
		return nil
	}
	return s.db.WithContext(ctx).Omit(clause.Associations).Create(rows).Error
}

// Loads one row from reviews
func (s *Store) GetReview(ctx context.Context, id int64) (*testv1.Review, error) {
	var row testv1.Review
	if err := s.db.WithContext(ctx).First(&row, "id = ?", id).Error; err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return nil, fmt.Errorf("review not found: %w", gorm.ErrRecordNotFound)
		}
		return nil, err
	}
	return &row, nil
}

// Lists rows from reviews
func (s *Store) ListReviews(ctx context.Context) ([]*testv1.Review, error) {
	var rows []*testv1.Review
	if err := s.db.WithContext(ctx).Find(&rows).Error; err != nil {
		return nil, err
	}
	return rows, nil
}

// Patches columns on one reviews row
func (s *Store) UpdateReviewFields(ctx context.Context, id int64, fields map[string]any) error {
	if len(fields) == 0 {
		return nil
	}
	res := s.db.WithContext(ctx).Model(&testv1.Review{}).Where("id = ?", id).Updates(fields)
	if res.Error != nil {
		return res.Error
	}
	if res.RowsAffected == 0 {
		return fmt.Errorf("review not found: %w", gorm.ErrRecordNotFound)
	}
	return nil
}

// Deletes one row from reviews
func (s *Store) DeleteReview(ctx context.Context, id int64) error {
	return s.db.WithContext(ctx).Delete(&testv1.Review{}, "id = ?", id).Error
}
